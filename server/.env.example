# Server environment variables
# Copy this to server/.env and set values as needed

# Port the server should listen on (defaults to 3000)
PORT=3000

# GitHub token (optional) to increase API rate limits and access private resources
# Create a token at https://github.com/settings/tokens with 'repo' or 'public_repo' scopes as needed
GITHUB_TOKEN=your_github_token_here
